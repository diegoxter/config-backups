set -g default-terminal "xterm-256color"
#set-option -ga terminal-overrides ",xterm-256color:Tc"
set -g mouse on

unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# Vim style pane selection
bind h select-pane -L
bind j select-pane -D 
bind k select-pane -U
bind l select-pane -R

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

#######################################################
##                  Visual settings                  ##
#######################################################

set -g status on
set -g status-style 'fg=white,bg=black'
set -g status-left "#[fg=green]H: #[fg=cyan]#H #[fg=green]/ #[fg=yellow]S: #S #[default]"

# remote session management
# Detección de entorno
#is_local='[ "$(hostname)" = "La-PC" ]'
is_ssh='[ -n "$SSH_CLIENT" ]'

# Estilo para local

#set -g  status-left "#[fg=green]Session: #> #[fg=yellow]#S"

# Sobreescribir si es local
if-shell "$is_ssh" {
  #set -g status-bg colour235
 # set -g status-bg colour52  # Por defecto (remoto)
  set -g status-left "#[fg=red]#(whoami)#[default]@ SSH "
}

set -g  status-left-length 40
set -g  status-right "#[fg=cyan]%d %b %R"

set -g window-status-style fg=cyan,bg=black
set -g window-status-current-style fg=white,bg=black

#######################################################

#set -g @catppuccin_flavour 'mocha'
## set -g @plugin 'dreamsofcode-io/catppuccin-tmux'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'

set -g @plugin 'tmux-plugins/tmux-yank'

run '~/.tmux/plugins/tpm/tpm'

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
